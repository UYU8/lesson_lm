#float只能向左或者向右移动 不能向上或者向下 导致元素脱离文档流
//让元素从左往右排列
console.log(li.offsetWidth);
console.log(li.offsetHeight);
console.log(li.offsetLeft);
console.log(li.offsetTop);
获取元素几何属性 宽度 高度 距离左侧的上侧的距离 没有右侧 是因为屏幕以左边为元素基点
push unshift shift pop splice